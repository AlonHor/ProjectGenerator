{
  "name": "--",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "format": "prettier -w \"src/**/*.ts\"",
    "format-all": "prettier -w .",
    "lint": "eslint --fix src/**/*.ts",
    "compile": "tsc -p tsconfig.json",
    "build": "yarn format && yarn lint && yarn compile",
    "start": "node dist/index.js",
    "dev": "nodemon src/index.ts",
    "open": "yarn build && npm start",
    "prepare": "husky install",
    "pre-commit-setup": "husky install && husky add .husky/pre-commit \"yarn build\" && husky add .husky/post-merge \"yarn && yarn build\" && git add .husky/pre-commit && git add .husky/post-merge && git commit -m \"chore: add pre-commit hook\"",
    "post-merge": "yarn && yarn build",
    "pre-commit": "yarn build"
  },
  "author": "--",
  "license": "MIT"
}
